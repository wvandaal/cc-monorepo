{
  "name": "cc-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "create-package": "tsx scripts/create-package.ts",
    "sync-refs": "tsx scripts/sync-references.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
